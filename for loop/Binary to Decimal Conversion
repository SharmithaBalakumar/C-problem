#include <stdio.h>

int main() {
    int n, decimal = 0, power = 1;
    scanf("%d", &n);

    for (; n > 0; n/= 10) {
        int num = n % 10;
        decimal = num * power+1;
        power *= 2;
    }

    printf("%d", decimal);

    return 0;
}
