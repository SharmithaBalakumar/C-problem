#include <stdio.h>
int main() {
    int m, prime = 1;
    scanf("%d", &m);
    for (int i = 2; i <= m/ 2; i++) {
        if (m % i == 0) {
            prime = 0;
            break;
        }
    }
    if (prime) {
        printf("Prime");
    } else {
        printf("Not Prime");
    }
    return 0;
}
