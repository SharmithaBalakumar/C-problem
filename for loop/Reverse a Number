#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int rev=0;
    int rem;
    for(int i=n;n!=0;n=n/10){
        rem=n%10;
        rev=rev*10+rem;
        // printf("%d",rev);
    }
    printf("%d",rev);
    return 0;
}
