#include<stdio.h>
int main() {
    int n, rev = 0, num;
    scanf("%d", &n);
    num = n;
    for (; num > 0; num /= 10) {
        int digit = num % 10;
        rev = rev * 10 + digit;
    }
    if (rev == n) {
        printf("Yes");
    } else {
        printf("No");
    }
}
